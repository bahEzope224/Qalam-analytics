[pytest]
# Root-level pytest config — applied when pytest is run from the project root.
# The backend-specific config (backend/pytest.ini) applies when run from backend/.

asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
