"""
Shared pytest fixtures for the backend test suite.

Usage:
    - async tests: use @pytest.mark.asyncio and the `async_client` fixture.
    - sync tests: use the `client` fixture (httpx.TestClient).
"""



# ---------------------------------------------------------------------------
# Future fixtures go here, e.g.:
#   - async DB session (AsyncSession)
#   - authenticated async_client
#   - factory fixtures for Site, User, MetricsSnapshot
# ---------------------------------------------------------------------------
